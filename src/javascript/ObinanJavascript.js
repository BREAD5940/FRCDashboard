function frontPiston(/*front piston extended boolean*/) {
    if (/*boolean var*/) {
        document.getElementById("FrontPiston").style.backgroundColor = "green";
    }
}

function backPiston(/*back piston extended boolean*/) {
    if (/*boolean var*/) {
        document.getElementById("BackPiston").style.backgroundColor = "green";
    }
}

function arm(/*arm extended boolean*/) {
    if (/*boolean var*/) {
        document.getElementById("Arm").style.backgroundColor = "green";
    }
}

function buddyClimb(/*buddy climb extended boolean*/) {
    if (/*boolean var*/) {
        document.getElementById("BuddyClimb").style.backgroundColor = "green";
    }
}

function buddyClimb(/*pawl activation boolean*/) {
    if (/*boolean var*/) {
        document.getElementById("Pawl").style.backgroundColor = "green";
    }
}

var countDownTime = new Date(/**/).getTime();
function timer() {
    document.getElementById("Time").innerHTML = ;
}